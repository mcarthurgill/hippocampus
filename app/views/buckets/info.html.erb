<main class="main-content" role="main" aria-label="Main Content">
  <div class="bucket-list-wrap sorted-list" id="bucket<%= @bucket.id %>">
    <div class="search-results-list"></div>
    <%= render 'shared/items/sorted_items', :items => @items, :bucket => @bucket %>
    <%= render 'shared/items/thought_entry_form' %>
  </div>
</main>

<%= render 'shared/modals/new_bucket_modal' %>        
<%= render 'shared/items/thought_entry_form_js' %>
